name: shared_preferences_service
description: A service class for the Shared preferences package in Quotify.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  shared_preferences: ^2.3.3

dev_dependencies:
  faker: ^2.2.0
  flutter_test:
    sdk: flutter
  lint: ^2.8.0
  shared_preferences_service_test: ^0.0.1
