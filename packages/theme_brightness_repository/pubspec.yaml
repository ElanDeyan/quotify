name: theme_brightness_repository
description: The logic and data layer for theme brightness.
version: 0.0.2

resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  quotify_utils: ^5.0.0
  shared_preferences_service: ^2.0.0

dev_dependencies:
  faker: ^2.2.0
  flutter_test:
    sdk: flutter
  lint: ^2.8.0
  mocktail: ^1.0.4
  mutation_test: ^1.7.0
  shared_preferences_service_test: ^0.0.1
