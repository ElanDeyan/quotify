name: quotify_utils
description: Utilities for the quotify project
version: 5.0.0
publish_to: 'none'

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  event: ^3.1.0
  logging: ^1.3.0
  meta: ^1.15.0
  synchronized: ^3.3.1

dev_dependencies:
  checks: ^0.3.0
  lint: ^2.8.0
  test: ^1.25.8
